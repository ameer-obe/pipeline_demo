# Shared configuration so your machine and the CI runner behave identically.

[tool.black]
line-length = 88          # black's default; CI checks code matches this

[tool.pytest.ini_options]
addopts = "-v"            # verbose test output
testpaths = ["tests"]     # where pytest looks for tests
pythonpath = ["."]        # so `from calculator import ...` resolves
